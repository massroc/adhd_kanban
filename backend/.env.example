# Django settings
DJANGO_SECRET_KEY=your-secret-key-change-in-production
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Database (PostgreSQL)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=kanban
DB_USER=kanban_user
DB_PASSWORD=kanban_dev_password
DB_HOST=localhost
DB_PORT=5432

# CORS (for Tauri desktop app - localhost:1420 is the default Tauri dev server port)
CORS_ALLOWED_ORIGINS=http://localhost:1420,http://127.0.0.1:1420

# Logging
DJANGO_LOG_LEVEL=INFO
